packages:
  hello:
    requires:
      - bcore
    alias: python
    arguments:
      prepend:
        - -c
        - import hello; hello.HelloWorldCommand.main()
    environment:
      variables:
        PYTHONPATH: '{process.bootstrap_dir}/../lib'
